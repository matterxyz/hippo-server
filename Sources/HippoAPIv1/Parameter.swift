/// Parameters used in the URL
enum Parameter {
  /// The object's client reference owner (the client's Cloudkit ID)
  static let clientReferenceOwner = "client_reference_owner"
  /// The object's client reference ID
  static let clientReferenceID = "client_reference_id"
}